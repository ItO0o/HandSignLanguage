using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FingerParternSetter : MonoBehaviour
{

    struct FingerValue
    {
        public float thumb;
        public float index;
        public float middle;
        public float ring;
        public float little;
    }

    List<FingerValue> fingerPoses;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void PaperPose()
    {
        //FingerValue temp = new FingerValue();
        //temp.thumb = 1;
        //temp.index = 1;
        //temp.middle = 1;
        //te
    }
}
